[build]
  command = "rm -rf _flutter && git clone https://github.com/flutter/flutter.git --branch stable --depth 1 _flutter && export PATH=\"$PATH:`pwd`/_flutter/bin\" && flutter build web --release"
  publish = "build/web"

[build.environment]
  FLUTTER_WEB_USE_SKIA = "true"